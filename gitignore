# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.so
.env
.venv
venv/
ENV/
env/

# Jupyter Notebooks
.ipynb_checkpoints/
*.ipynb_checkpoints

# nTop — exclude large binary outputs but keep .ntop notebooks
geometry/exports/*.stl
geometry/exports/*.step
geometry/exports/*.stp
ntop_runs/

# ANSYS — result files are huge, don't commit them
simulation/results/*.rst
simulation/results/*.rth
simulation/results/*.rfl
simulation/results/*.db
*.err
*.out
*.log
ansys.lock

# Data — raw outputs can be large; commit only processed CSVs
data/raw/
data/results/

# VS Code
.vscode/
*.code-workspace

# Windows
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# MacOS (if you ever switch)
.DS_Store
.AppleDouble
.LSOverride

# Large model checkpoints
ml/surrogate/*.pkl
ml/gnn/checkpoints/
*.pt
*.pth
